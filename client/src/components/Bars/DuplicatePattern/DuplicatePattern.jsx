import React from 'react';
import Button from '../../UI/Button';

const DuplicatePattern = ({ handleClick }) => (
  <Button type="button" onClick={handleClick}>Duplicate Pattern</Button>
);

export default DuplicatePattern;
